// Re-exports from `$lib` go here. Right now Contactly is a fresh project
// so there's nothing to re-export — this file exists so `$lib` resolves
// without a missing-module error before the first lesson adds anything
// real.
export {};
